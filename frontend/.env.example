# URL base de la API Express, accesible desde el navegador del usuario
# (por eso usa localhost y el puerto publicado, NO el nombre del servicio Docker).
VITE_API_URL=http://localhost:4000/api

# URL del servidor Socket.IO (mismo backend)
VITE_SOCKET_URL=http://localhost:4000
